language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t raviyad/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true raviyad/docker-react npm run test
